import { css } from '@emotion/native'

export const styles = {
  container: css`
    width: 100%;
    height: 40px;
    padding: 10px;

    display: flex;
    flex-direction: row;

    justify-content: flex-end;
    align-items: center;
  `,
}
